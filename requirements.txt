# Streamlit app framework
streamlit==1.28.0

# sentencepiece (tokenizers)
sentencepiece==0.1.99


# Use the official CPU-only PyTorch wheel (prebuilt) to avoid source builds
torch==2.0.1+cpu
-f https://download.pytorch.org/whl/cpu/torch_stable.html

# Numerical libs
numpy>=1.21.0,<1.25.0

# utilities
tqdm>=4.62.0
psutil>=5.8.0
